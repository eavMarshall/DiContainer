language: php

sudo: false

notifications:
  email:
    on_success: never

php:
  - 7.0
  - 7.1
  - 7.2
  - nightly

install:
  - composer require --dev php-coveralls/php-coveralls

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer install -n

script:
  - mkdir -p build/logs
  - vendor/bin/phpunit --bootstrap vendor/autoload.php tests/UnitTest/tests.php --coverage-clover build/logs/clover.xml

after_success:
  - travis_retry php vendor/bin/php-coveralls